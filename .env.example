# Override the container port if 8002 is taken.
# PORT=8002

# Where Helsinki archives land inside the container (keep default for bind-mount).
# HELSINKI_OUT_DIR=/app/data/helsinki-out

# HuggingFace cache — reuse an existing cache by volume-mounting into ./data/hf-cache.
# HF_HOME=/root/.cache/huggingface
